<h1>Editing Book</h1>

<%= form_with model: @book, url: books_path,method: :post do |f| %>

<h4>タイトル</h4>
<%= f.text_field :title %>

<h4>本の感想</h4>
<%= f.text_area :body %>

<%= f.submit 'Create Book' %>

<% end %>